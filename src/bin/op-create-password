#!/usr/bin/env zsh

ITEM_NAME="${1:-$(date '+%Y-%m-%dT%H:%M:%S')}"

op item create --title="$ITEM_NAME" --category=password --generate-password=20,letters,digits,symbols | op read "op://Private/'$ITEM_NAME'/password"
