% python, snippet

# Embed IPython terminal for debugging
from IPython import embed; embed()

% python, tools

# Cleanup empty python directories
find . -type f -name "*.pyc" -delete && find . -type d -name "__pycache__" -delete && find . -type d -name ".pytest_cache" -delete && find . -type d -empty -delete
