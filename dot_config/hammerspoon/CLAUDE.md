# Hammerspoon Config

After making changes to files in this directory, run `chezmoi apply` to deploy changes.

The `run_onchange_after_reload-hammerspoon.sh.tmpl` script runs automatically when `init.lua` or any Spoon's `init.lua` changes, reloading Hammerspoon via CLI or AppleScript.
