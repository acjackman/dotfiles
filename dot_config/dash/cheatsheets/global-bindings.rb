cheatsheet do
  title 'Global Bindings'
  docset_file_name 'Global_Bindings'
  keyword 'bind'

  introduction 'Global keyboard shortcuts and bindings across tools'

  category do
    id 'Dash'

    entry do
      command 'Shift+Ctrl+Opt+Cmd+H'
      name 'Search Dash'
    end
    entry do
      command 'Shift+Ctrl+Opt+Cmd+Y'
      name 'Search Dash using selected text'
    end
  end

  category do
    id 'TextSniper'

    entry do
      command 'Cmd+Shift+2'
      name 'Capture text'
    end
    entry do
      command 'Cmd+Shift+Ctrl+2'
      name 'Capture text without line breaks'
    end
  end
end
