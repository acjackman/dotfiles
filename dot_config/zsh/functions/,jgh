# -*- mode: zsh -*- vim: ft=zsh

# ,jgh - Jump Git Home
#
# Pushd to the root of the current worktree

if [[ "$(git rev-parse --is-inside-work-tree)" != "true" ]]; then
  echo "Not in a git repository" >&2
  return 1
fi

local dir
dir="$( git rev-parse --show-toplevel )"

pushd "${dir}" >/dev/null
