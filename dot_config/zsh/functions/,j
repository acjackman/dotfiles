# -*- mode: zsh -*- vim: ft=zsh

# ,j - Jump
#
# Pushd to an fzf-selected directory

local items
items=$(find ${1:-.} -path '*/\.*' -prune -o -type d -print 2> /dev/null)

local dir
dir=$(,j-fzf-picker "Jump to directory: " "$items")
[[ -n "$dir" ]] && pushd "$dir" >/dev/null
