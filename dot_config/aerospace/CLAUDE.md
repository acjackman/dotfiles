# Aerospace Config

After making changes to files in this directory, run `chezmoi apply` to deploy changes.

The `run_onchange_after_reload-aerospace.sh.tmpl` script runs automatically when `aerospace.toml` changes, reloading the aerospace window manager config via `aerospace reload-config`.
