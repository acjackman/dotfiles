#!/bin/bash
# Runs inside a new Ghostty window — launches the sesh session picker.
# Called via: open -a Ghostty.app --args -e <this-script>

printf '\033c'
TMUX= exec ~/.config/tmux/tmux_launcher.sh
