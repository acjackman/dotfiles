- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- shell:
  - git submodule sync --recursive

- link:
    ~/.hammerspoon:
      if: '[ `uname` = Darwin ]'
      path: src/hammerspoon
    ~/.zshrc:
      force: true
      path: src/.zshrc

